<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeBot | The AI Coding Revolution</title>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500&family=Plus+Jakarta+Sans:wght@300;400;600;800&display=swap" rel="stylesheet">
    <style>
        :root {
            /* Dark Theme (Re-designed for Premium Matte Look) */
            --bg: #0a0a0a;
            --surface: #141414;
            --text: #ffffff;
            --text-dim: #a1a1aa;
            --primary: #22c55e;
            --primary-glow: rgba(34, 197, 94, 0.15);
            --accent: #3b82f6;
            --border: #27272a;
            --glass: rgba(10, 10, 10, 0.8);
        }

        [data-theme="light"] {
            --bg: #ffffff;
            --surface: #f4f4f5;
            --text: #09090b;
            --text-dim: #71717a;
            --primary: #16a34a;
            --primary-glow: rgba(22, 163, 74, 0.1);
            --accent: #2563eb;
            --border: #e4e4e7;
            --glass: rgba(255, 255, 255, 0.8);
        }

        * {
            margin: 0; padding: 0; box-sizing: border-box; scroll-behavior: smooth;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: var(--bg);
            color: var(--text);
            transition: background 0.5s ease;
            overflow-x: hidden;
        }

        /* Responsive Navbar */
        nav {
            position: fixed; top: 0; width: 100%;
            display: flex; justify-content: space-between; align-items: center;
            padding: 1rem 5%;
            background: var(--glass);
            backdrop-filter: blur(12px);
            z-index: 1000;
            border-bottom: 1px solid var(--border);
        }

        .logo { font-size: 1.4rem; font-weight: 800; color: var(--primary); display: flex; align-items: center; gap: 8px; }
        
        .nav-links { display: flex; gap: 2rem; align-items: center; }
        .nav-links a { text-decoration: none; color: var(--text); font-weight: 500; font-size: 0.9rem; opacity: 0.8; transition: 0.3s; }
        .nav-links a:hover { opacity: 1; color: var(--primary); }

        /* Hamburger Menu Icon */
        .mobile-menu-btn {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
            background: none;
            border: none;
            z-index: 1100;
        }

        .mobile-menu-btn span {
            width: 25px;
            height: 2px;
            background-color: var(--text);
            transition: 0.3s;
        }

        /* Theme Toggle */
        #theme-toggle {
            background: var(--surface);
            border: 1px solid var(--border);
            color: var(--text);
            padding: 6px 14px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
        }

        /* Hero Section */
        .hero {
            padding: 160px 5% 100px;
            text-align: center;
            background: radial-gradient(circle at 50% -20%, var(--primary-glow), transparent 50%);
        }

        .hero h1 { 
            font-size: clamp(2.5rem, 8vw, 5rem); 
            font-weight: 800; 
            letter-spacing: -3px; 
            line-height: 1;
            margin-bottom: 20px;
        }

        .hero p { 
            max-width: 650px; margin: 0 auto 40px; 
            font-size: 1.2rem; color: var(--text-dim);
            line-height: 1.6;
        }

        .cta-group { display: flex; gap: 15px; justify-content: center; flex-wrap: wrap; }
        .btn { padding: 12px 30px; border-radius: 10px; font-weight: 700; cursor: pointer; transition: 0.3s; text-decoration: none; border: none; }
        .btn-primary { background: var(--primary); color: white; box-shadow: 0 10px 20px -5px var(--primary-glow); }
        .btn-secondary { background: var(--surface); color: var(--text); border: 1px solid var(--border); }
        .btn:hover { transform: translateY(-2px); filter: brightness(1.1); }

        /* Floating Stats Section */
        .stats-section {
            display: flex; justify-content: center; gap: 40px; flex-wrap: wrap;
            padding: 40px 5%;
        }
        .stat-item { text-align: center; }
        .stat-item h2 { font-size: 2.5rem; color: var(--text); }
        .stat-item p { color: var(--primary); font-weight: 600; font-size: 0.9rem; text-transform: uppercase; }

        /* Terminal Window */
        .terminal-container { padding: 0 5%; margin-bottom: 100px; }
        .terminal {
            max-width: 900px; margin: 0 auto;
            background: #000; border-radius: 12px;
            border: 1px solid var(--border); overflow: hidden;
            box-shadow: 0 50px 100px -20px rgba(0,0,0,0.5);
        }
        .term-head { background: #1a1a1a; padding: 12px; display: flex; gap: 8px; border-bottom: 1px solid #333; }
        .dot { width: 12px; height: 12px; border-radius: 50%; }
        .term-body { padding: 25px; font-family: 'Fira Code', monospace; color: #a1a1aa; font-size: 14px; }

        /* Features Section */
        .section { padding: 100px 5%; }
        .section-header { text-align: center; margin-bottom: 60px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; }
        .card { 
            background: var(--surface); padding: 40px; border-radius: 20px; 
            border: 1px solid var(--border); transition: 0.3s;
        }
        .card:hover { border-color: var(--primary); background: var(--bg); }
        .card h3 { margin-bottom: 15px; font-size: 1.4rem; }
        .card p { color: var(--text-dim); }

        /* Language Slider */
        .slider-container { background: var(--surface); padding: 30px 0; overflow: hidden; white-space: nowrap; border-top: 1px solid var(--border); }
        .slider { display: inline-block; animation: slide 20s linear infinite; }
        .slider span { font-size: 1.5rem; font-weight: 800; margin: 0 40px; color: var(--text-dim); opacity: 0.4; }
        @keyframes slide { from { transform: translateX(0); } to { transform: translateX(-50%); } }

        /* Testimonials */
        .testimonials { background: var(--bg); }
        .test-card { font-style: italic; font-size: 1.1rem; border-left: 4px solid var(--primary); padding-left: 20px; }

        /* Footer */
        footer { 
            padding: 80px 5% 40px; background: var(--surface); border-top: 1px solid var(--border);
        }
        .footer-grid { 
            display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 40px; margin-bottom: 60px;
        }
        .footer-col h4 { margin-bottom: 20px; font-weight: 700; }
        .footer-col a { display: block; text-decoration: none; color: var(--text-dim); margin-bottom: 10px; font-size: 0.9rem; }
        .copyright { text-align: center; color: var(--text-dim); font-size: 0.8rem; padding-top: 40px; border-top: 1px solid var(--border); }

        /* Mobile Menu */
        @media (max-width: 768px) {
            .mobile-menu-btn { display: flex; }
            
            .nav-links { 
                display: none; /* Hide standard links */
                flex-direction: column;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: var(--surface);
                padding: 20px;
                gap: 15px;
                border-bottom: 1px solid var(--border);
                box-shadow: 0 10px 15px rgba(0,0,0,0.2);
            }

            .nav-links.active { display: flex; } /* Toggle class */

            .nav-links .btn { width: 100%; text-align: center; }
            #theme-toggle { width: 100%; }

            .hero h1 { font-size: 3rem; }
            .section { padding: 60px 5%; }
        }
    </style>
</head>
<body data-theme="dark">

    <nav>
        <div class="logo"><span>⚡</span> CodeBot</div>
        
        <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <div class="nav-links" id="navLinks">
            <button id="theme-toggle" onclick="toggleTheme()">☀️ Light Mode</button>
            <a href="/login" class="btn btn-secondary" style="padding: 8px 20px;">Login</a>
            <a href="/signup" class="btn btn-secondary" style="padding: 8px 20px;">Signup</a>
        </div>
    </nav>

    <header class="hero">
        <h1>Your AI <span>Code-Bot</span> for the Future.</h1>
        <p>CodeBot doesn't just write code; it thinks like a developer. Debug, refactor, and deploy with the world's most advanced AI engine.</p>
        <div class="cta-group">
            <a href="/demo" class="btn btn-primary">Start Chatting Free</a>
            <a href="#features" class="btn btn-secondary">Explore Features</a>
        </div>
    </header>

    <section class="stats-section" id="stats">
        <div class="stat-item"><h2>10M+</h2><p>Lines Fixed</p></div>
        <div class="stat-item"><h2>50k+</h2><p>Active Devs</p></div>
        <div class="stat-item"><h2>99%</h2><p>Accuracy Rate</p></div>
        <div class="stat-item"><h2>24/7</h2><p>Live Support</p></div>
    </section>

    <div class="terminal-container">
        <div class="terminal">
            <div class="term-head">
                <div class="dot" style="background: #ff5f56;"></div>
                <div class="dot" style="background: #ffbd2e;"></div>
                <div class="dot" style="background: #27c93f;"></div>
            </div>
            <div class="term-body">
                <p style="color: #22c55e;">> codebot --refactor profileController.js</p>
                <p>> Analyzing dependencies...</p>
                <p style="color: #3b82f6;">> Optimization found: Reduced database calls by 3.5x.</p>
                <p>> Applying fixes... [Done]</p>
            </div>
        </div>
    </div>

    <div class="slider-container">
        <div class="slider">
            <span>REACT</span><span>PYTHON</span><span>DOCKER</span><span>NEXT.JS</span><span>RUST</span><span>NODE.JS</span><span>GO</span><span>AWS</span>
            <span>REACT</span><span>PYTHON</span><span>DOCKER</span><span>NEXT.JS</span><span>RUST</span><span>NODE.JS</span><span>GO</span><span>AWS</span>
        </div>
    </div>

    <section id="features" class="section">
        <div class="section-header">
            <h2>Powerful by Design</h2>
            <p>Built to handle the most complex enterprise codebases.</p>
        </div>
        <div class="grid">
            <div class="card">
                <h3>Intelligent Debugging</h3>
                <p>Paste code or paste an error. CodeBot traces the stack and finds the logic flaw in seconds.</p>
            </div>
            <div class="card">
                <h3>Code Documentation</h3>
                <p>Generate clean, readable JSDoc or Swagger docs automatically from your raw source code.</p>
            </div>
            <div class="card">
                <h3>Security Audit</h3>
                <p>Automatically scans for SQL injection, XSS, and broken auth patterns before you push to production.</p>
            </div>
        </div>
    </section>

    <section id="testimonials" class="section" style="background: var(--surface);">
        <div class="section-header">
            <h2>Trusted by the best</h2>
        </div>
        <div class="grid">
            <div class="card test-card">
                <p>"CodeBot saved us 40 hours of refactoring in the first week. It's like having a Senior Dev in my pocket."</p>
                <br><strong>- Sarah J., Lead Dev at Vercel</strong>
            </div>
            <div class="card test-card">
                <p>"The security audit feature is a lifesaver. It caught a major leak before we went live."</p>
                <br><strong>- Mike D., CTO</strong>
            </div>
        </div>
    </section>

    <section class="section" style="text-align: center;">
        <div class="card" style="max-width: 800px; margin: 0 auto; background: linear-gradient(to right, var(--primary-glow), transparent);">
            <h2>Ready to ship faster?</h2>
            <p style="margin: 20px 0;">Join 50,000+ developers building the future with CodeBot.</p>
            <a href="/signup" class="btn btn-primary">Get Started Now</a>
        </div>
    </section>

    <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <div class="logo">⚡ CodeBot</div>
                <p style="margin-top: 15px;">The only AI partner you'll ever need.</p>
            </div>
            <div class="footer-col">
                <h4>Product</h4>
                <a href="#">Security</a>
                <a href="#">Extension</a>
                <a href="#">CLI Tool</a>
            </div>
            <div class="footer-col">
                <h4>Company</h4>
                <a href="#">Careers</a>
                <a href="#">Blog</a>
                <a href="#">Privacy</a>
            </div>
            <div class="footer-col">
                <h4>Support</h4>
                <a href="https://www.linkedin.com/in/gupta-aniket5/">LinkedIn</a>
            </div>
        </div>
        <p class="copyright">&copy; 2026 CodeBot AI Inc. All rights reserved.</p>
    </footer>

    <script>
        function toggleTheme() {
            const body = document.body;
            const btn = document.getElementById('theme-toggle');
            if (body.getAttribute('data-theme') === 'dark') {
                body.setAttribute('data-theme', 'light');
                btn.innerHTML = "🌙 Dark Mode";
            } else {
                body.setAttribute('data-theme', 'dark');
                btn.innerHTML = "☀️ Light Mode";
            }
        }

        // New function for mobile menu
        function toggleMobileMenu() {
            const nav = document.getElementById('navLinks');
            nav.classList.toggle('active');
        }
    </script>
</body>
</html>